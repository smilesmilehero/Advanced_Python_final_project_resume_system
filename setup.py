from setuptools import setup
setup(
    name="my-backend",
    version="1.0",
    description="final project",
    packages=[
        "backend",
        "backend.entity",
        "backend.repo",
        "backend.testrepo",
    ],
    install_requires=[

    ]
)

